import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.escolaapp.mobile',
  appName: 'Escola App Mobile',
  webDir: 'dist/public'
};

export default config;
